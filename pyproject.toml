[project]
name = "python-library-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Rob Enderle", email = "robenderleamd@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
python-library-test = "python_library_test:main"
draft-optimizer = "draft_optimizer.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/python_library_test", "src/draft_optimizer"]
